module.exports = {
  // jest-environment-jsdom is actually a node module that is installed automatically for you with jest
  // there is also one for node
  testEnvironment: 'jest-environment-jsdom',
}
